services:
  app:
    build:
      target: development
    env_file:
      - .env
    environment:
      NODE_ENV: development
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run start:dev
    restart: no

  db:
    ports:
      - "${DB_PORT}:${DB_PORT}"
